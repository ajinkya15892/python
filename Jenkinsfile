pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version >> /tmp/python.txt'
            }
        }
post {
echo "feature branch" 
}
        }
}
